import { chromium } from "@playwright/test";
import { fileURLToPath } from "node:url";
import path from "node:path";
import os from "node:os";
const dir = path.dirname(fileURLToPath(import.meta.url));
const out = path.join(os.homedir(), "Desktop", "Docto-IG");
const browser = await chromium.launch();
const page = await browser.newPage({ viewport: { width: 1080, height: 1350 }, deviceScaleFactor: 2 });
await page.goto("file://" + path.join(dir, "placa4.html"));
await page.evaluate(() => document.fonts.ready);
await page.waitForTimeout(500);
await page.locator("#s4").screenshot({ path: path.join(out, "MUESTRA-receta-v1.png") });
console.log("OK → MUESTRA-receta-v1.png");
await browser.close();
